# Normalise text files to LF in the repository and in the working tree, so the
# project produces identical diffs on Windows, macOS and Linux.
* text=auto eol=lf

# Binary assets: never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
